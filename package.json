{
  "name": "explorevent",
  "description": "Host or Create Events on Explorevent",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "author": "Leon Yanagida <contact@leonyanagida.com>",
  "scripts": {
    "build": "concurrently \"cd client && yarn build\" \"cd server && yarn build\"",
    "start": "cd server && yarn start",
    "install": "(cd client && yarn) && (cd server && yarn)"
  },
  "engines": {
    "node": ">=12.0.0",
    "yarn": ">=1.22.*"
  },
  "dependencies": {
    "@types/node": "^14.6.4",
    "concurrently": "^5.3.0",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.2",
    "yarn": "^1.22.5"
  }
}
